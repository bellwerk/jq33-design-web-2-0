console.error(
  "Legacy runtime configuration has been retired. The public site is built as static-only output with fail-closed Formspree, Calendly, and Cloudflare Web Analytics inputs.",
);
process.exit(1);
